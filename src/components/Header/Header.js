import { HeaderBar } from './Header.styled';
import { SharedLayout } from '../SharedLayout/SharedLayout';

export const Header = () => {
  return (
    <HeaderBar>
      <SharedLayout />
    </HeaderBar>
  );
};

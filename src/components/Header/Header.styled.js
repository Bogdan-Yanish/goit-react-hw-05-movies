import styled from 'styled-components';

export const HeaderBar = styled.header`
  padding: 20px 40px;
  box-shadow: 0px 8px 35px -4px rgba(245, 76, 39, 0.8);
  border-bottom: 1px solid ${p => p.theme.colors.border};
`;
